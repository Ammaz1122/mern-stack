

let rectangle = (width,height)=>{
   return width * height;
   
}
 let width = prompt("Enter a value of width ");
 let height = prompt(" Enter the height of Rectangle ");
 let rec = rectangle(width,height);
 alert(`Rectagle of ${width} and ${height} is: ${rec}`)
